// @flow
/*declare module 'rxjs' {
  declare class Observable<T> {
    subscribe(
      next?: (value: T) => void,
      error?: (error: any) => void,
      complete?: () => void
    ): { unsubscribe: () => void };
    // Add other methods as needed
  }
	declare export function of<T>(...args: Array<T>): Observable<T> | any;
  declare export { Observable };
}*/
